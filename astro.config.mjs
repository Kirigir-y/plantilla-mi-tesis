// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Descomenta y edita esto cuando subas a GitHub Pages:
 site: 'https://kirigir-y.github.io',
  base: '/plantilla-mi-tesis',
});
